# Audio assets

- `dynamax-intro.ogg` — short orchestral hit that plays before any `-gmax` /
  `-eternamax` Pokémon's cry (chained via `Audio.onended` in `PokemonCard.tsx`).
  Sourced from a Sword/Shield rip; drop your own clip here. Either `.ogg` or
  `.mp3` is fine — Vite serves the file as-is at `/audio/dynamax-intro.ogg`.
